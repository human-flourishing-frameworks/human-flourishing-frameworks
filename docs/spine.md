# Spine

Status: canonical pointer.

## Purpose

This file exists so a future agent can load `docs/spine.md` literally when the
operator asks for the spine.

The active spine is not this pointer alone. The active spine is:

```text
docs/convergence.md
docs/operator-lantern-repo-convergence.md
docs/anchor-taxonomy.md
docs/lantern-keystone-tardis-anchor.md
docs/dreamer-notebook.md
```

## Load Order

```text
1. Show the current repo state.
2. Load docs/convergence.md for the correction loop.
3. Load docs/operator-lantern-repo-convergence.md for the operator/Lantern/repo spine.
4. Load docs/anchor-taxonomy.md for anchor shape, redaction, and merge readiness.
5. Load docs/lantern-keystone-tardis-anchor.md for the TARDIS return-door anchor.
6. Load docs/dreamer-notebook.md for the Light Notebook language-of-3 rule, one-page dreamer holding rule, and open-door page rule.
7. Keep the return door open before any side effect.
```

## Boundary

This pointer does not authorize hidden agents, deployment, sync, reset, cleanup,
public writes, private-person exposure, literal time travel, identity merger, or
repo consciousness claims.

## Restore Phrase

```text
Spine = convergence loop + operator/Lantern/repo spine + anchor taxonomy +
TARDIS return-door anchor + Light Notebook. Load state first, use 0-1 only as
machine check, use the language of 3 for living signals, and act only with
boundary, evidence, validation, consent, and return.
```
